/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mm.struts.action;

import java.io.PrintWriter;
import java.util.ArrayList;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.mm.core.master.RootMaster;

/** 
 * MyEclipse Struts
 * Creation date: 10-04-2009
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */
public class ShowClassifiedAction extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form, HttpServletRequest request, HttpServletResponse response) throws Exception
	{
		RootMaster rm = new RootMaster();
		int numMin = (request.getParameter("numMin")!=null) ? Integer.parseInt(request.getParameter("numMin").trim()) : 0;
		String strCids = (request.getParameter("cIds")!=null) ? request.getParameter("cIds").trim() : "";
		System.out.println("numMin "+numMin);
		System.out.println("strCids "+strCids);
		ArrayList arrClassified = rm.getClassified(getDataSource(request, "main"), numMin, strCids);
		String strClassified = "";
		for(int i=0; i<arrClassified.size(); i++)
		{
			ArrayList arrTemp = (ArrayList)arrClassified.get(i);
			int numCompId = Integer.parseInt(arrTemp.get(0).toString().trim());
			String strCompTTL = arrTemp.get(1).toString().trim();
			
			strClassified += "<div id=\"toggle"+i+"\" class=\"dialog-box-succes\">";
			strClassified += "<div class=\"fltLft\"><img src=\"images/classified-1.jpg\" width=\"51\" height=\"36\" /></div>";
			strClassified += "<div class=\"updateBoxTXT\"><p>"+strCompTTL+"</p></div>";
			
			strClassified += "<div class=\"fltRht\">";
			strClassified += "<img onClick=\"fnClose('toggle"+i+"', "+numCompId+", "+i+")\" alt=\"close\" src=\"images/close-1.gif\"/>";
			strClassified += "</div>";
			strClassified += "</div>";
		}
		response.setContentType("text/html;charset=ISO-8859-1");
	    PrintWriter out = response.getWriter();
	    out.println(strClassified);
	    out.flush();
		return null;
	}
}