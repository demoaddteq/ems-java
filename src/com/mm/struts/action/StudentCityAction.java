/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mm.struts.action;

import java.io.PrintWriter;
import java.util.ArrayList;
import java.util.HashMap;


import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
//import javax.sql.DataSource;
import com.mm.core.master.RootMaster;

/** 
 * MyEclipse Struts
 * Creation date: 06-30-2007
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */
public class StudentCityAction extends Action {
	RootMaster rootMaster= new RootMaster();
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) throws Exception {
		HttpSession session = request.getSession();
		int numSId = (request.getParameter("collstate")!=null) ? Integer.parseInt(request.getParameter("collstate").trim()) : 0;
		int numPId = (request.getParameter("collplace")!=null) ? Integer.parseInt(request.getParameter("collplace").trim()) : 0;
		String strCIdOth = (request.getParameter("strCIdOth")!=null) ? request.getParameter("strCIdOth").trim() : "";
		String strSIdOth = (request.getParameter("strSIdOth")!=null) ? request.getParameter("strSIdOth").trim() : "";
		String strPIdOth = (request.getParameter("strPIdOth")!=null) ? request.getParameter("strPIdOth").trim() : "";
		
		String strPlace = "";
		int tabindex =7;
		
		System.out.println("CollRegisCityAction....................");
		System.out.println("numStateId in place>......................."+numSId);
		System.out.println("numPId in place>......................."+numPId);
		System.out.println("strCIdOth in place>....................."+strCIdOth);
		System.out.println("strSIdOth in place>....................."+strSIdOth);
		System.out.println("strPIdOth in place>....................."+strPIdOth);
		
		
		if((request.getParameter("collstate")!=null)&& (numSId!=0 && numSId!=-1))
		{	
			//System.out.println("if...@@@@@@@@@@@@@@...........");
			
			
			HashMap citiesList = (HashMap)session.getAttribute("AllCities");
			strPlace = getPlaceList(numPId, citiesList, numSId,tabindex);
		}
		else
		{	
			//System.out.println("else...@@@@@@@@@@@@@@...........");
			
			strPlace ="<select name=\"companyCity\" tabindex=\"11\" onclick=\"showOth();\">";
			strPlace += "<option value=\"0\" Selected>Select</option>";
			strPlace += "<option value=\"-1\">Others</option>";
			strPlace += "</select>";
			
		}
		
		
		
	    response.setContentType("text/html;charset=ISO-8859-1");
	    PrintWriter out = response.getWriter();
	    out.println(strPlace);
	    out.flush();
		return null;
	}
	
	
	
	private String getPlaceList(int numPId, HashMap citiesList,  int numSId,int tabindex)
	{
		
		
		ArrayList AllCities = (ArrayList)citiesList.get(numSId);
		
		String strResult ="<select name=\"companyCity\"  tabindex=\""+tabindex+"\" onchange=\"showOth();\">";
			System.out.println("AllCities.size();>....................."+AllCities.size());
			for(int i=0; i<AllCities.size(); i++)
			{
				ArrayList CitiesArr = (ArrayList)AllCities.get(i);
				int numPlaceId = Integer.parseInt(CitiesArr.get(0).toString().trim());
				String strPName = CitiesArr.get(1).toString().trim();
			if(numPId == numPlaceId)
			{
				strResult += "<option value=\"" + numPlaceId + "\" Selected>" + strPName + "</option>";
			}
			else
			{
				strResult += "<option value=\"" + numPlaceId + "\">" + strPName + "</option>";
			}
		}
		strResult += "</select>";
		return strResult;
	}
	
}