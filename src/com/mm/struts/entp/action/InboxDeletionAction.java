/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mm.struts.entp.action;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionErrors;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.apache.struts.action.ActionMessages;
import org.apache.struts.action.ActionMessage;
import com.mm.core.master.EntpMaster;
import java.util.Vector;
import com.mm.struts.entp.form.InboxDeletionForm;

/** 
 * MyEclipse Struts
 * Creation date: 07-19-2007
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */
public class InboxDeletionAction extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		
		HttpSession session = request.getSession();
		String numCount = (String)session.getAttribute("numCount");
		int count = Integer.parseInt(numCount);
		int remander = count%2;
		//System.out.println("remander.............InboxDeletionAction........... "+remander);
		
		String uId =(String)session.getAttribute("uId");
		String compId =(String)session.getAttribute("compId");
		Vector allRights =(Vector)session.getAttribute("allRights");
		String adminFlag = allRights.elementAt(18).toString();
		
		EntpMaster entpMaster= new EntpMaster();		
		

		
		////System.out.println("dc in action >>>>>");
				
		
		String status = "failure";
		InboxDeletionForm inboxDeletionForm = (InboxDeletionForm)form;
		String[] inboxIds = inboxDeletionForm.getInboxids();
		////System.out.println("dc in action inboxIds >>>>>"+inboxIds);
		String inboxs = "";
		for(int i=0; i<inboxIds.length; i++)
		{
			////System.out.println("manoj in action "+i+">>>>>"+inboxIds[i]);
			if(i!=(inboxIds.length-1))
				inboxs = inboxs+inboxIds[i]+",";
			else
			{
				inboxs = inboxs+inboxIds[i];
			}
		}
		Vector<String> paramVec = new Vector<String>();
		String result = "failure";
		////System.out.println("inboxs>>>>>>>>>>>"+inboxs);
		if(inboxs.length()>0)
		{
			paramVec.add(inboxs);
			result = entpMaster.updateInbox(getDataSource(request, "entp"), paramVec);
		}
		
		if(result.equalsIgnoreCase("success"))
		{
			
			ActionErrors errors = new ActionErrors();
			errors.clear();
			errors.add("inboxdeleteentp", new ActionMessage("entp.inbox.DeleteSuccess"));
			
			saveErrors(request, errors);
			
			
			status="success";
		}
		else
		{
			ActionErrors errors = new ActionErrors();
			errors.clear();
			errors.add("inboxdeleteentp", new ActionMessage("entp.inbox.DeleteFailure"));
			
			saveErrors(request, errors);
			
			status ="failure";
		}
		
		
		//System.out.println("result>>>>>>>>>>>"+result);
		Vector dataVec3 = new Vector();		
		dataVec3.add(uId);
		dataVec3.add(compId);	
		dataVec3.add(adminFlag);
		Vector entpCountVec = entpMaster.complaintCountEntp(getDataSource(request, "entp"), dataVec3);		
		session.setAttribute("entpCountVec", entpCountVec);						
		//System.out.println("entpCountVec.............InboxDeletionAction........... "+entpCountVec);
		
		return mapping.findForward(status);
	}
}