/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mm.struts.entp.action;

import java.util.HashMap;
import java.util.Map;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;
import org.apache.struts.actions.LookupDispatchAction;
import org.apache.struts.action.ActionError;
import org.apache.struts.action.ActionErrors;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import java.util.Vector;

/** 
 * MyEclipse Struts
 * Creation date: 07-26-2007
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */

public class PrintComplaintAction extends LookupDispatchAction {
	/*
	 * Generated Methods
	 */
	protected Map getKeyMethodMap()
	{
		Map<String , String> map = new HashMap<String , String> ();
		map.put("print.mail", "sendMail");
		map.put("print.finish", "finish");
		return map;
	}

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward sendMail(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		String status = "send";
		
		//System.out.println("manoj in send mail start");
		HttpSession session = request.getSession();
		String pageId = (String)session.getAttribute("pageid");
		Vector tempvec = (Vector)session.getAttribute("tempvec");
		Vector mailVec = (Vector)session.getAttribute("mailVec");
		String CopmId = (String)session.getAttribute("CopmId");
		String fCopmId = (String)session.getAttribute("fCopmId");
		
		//System.out.println("tempvec>>>>>>>>>>>"+tempvec);
		//System.out.println("mailVec>>>>>>>>>>>"+mailVec);
		//System.out.println("CopmId>>>>>>>>>>>"+CopmId);
		//System.out.println("fCopmId>>>>>>>>>>>"+fCopmId);
		if(tempvec == null || mailVec == null || CopmId == null || fCopmId == null)
		{
			//System.out.println("manoj in send mail if");
			ActionErrors errors = new ActionErrors();
			errors.clear();
			errors.add("print", new ActionError("errors.print.failure"));
			
				saveErrors(request, errors);
			
			status="failure";
			if(Integer.parseInt(pageId)==7)
			{
				status = "searchFail";
			}
			else if(Integer.parseInt(pageId)==1)
			{
				status="failure";
			}
			else
			{
				status = "underFail";
			}
			
		}
		else
		{
			//System.out.println("manoj in send mail else");
				
			
		}
		// TODO Auto-generated method stub
		return mapping.findForward(status);
	}
	
	public ActionForward finish(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		//System.out.println("manoj in finish start");
		String status = "finish";
		HttpSession session = request.getSession();
		String pageId = (String)session.getAttribute("pageid");
		Vector tempvec = (Vector)session.getAttribute("tempvec");
		Vector mailVec = (Vector)session.getAttribute("mailVec");
		String CopmId = (String)session.getAttribute("CopmId");
		String fCopmId = (String)session.getAttribute("fCopmId");
		if(tempvec == null || mailVec == null || CopmId == null || fCopmId == null)
		{
			//System.out.println("manoj in finish if");
			
			ActionErrors errors = new ActionErrors();
			errors.clear();
			errors.add("print", new ActionError("errors.print.failure"));
			
				saveErrors(request, errors);
			
			
			if(Integer.parseInt(pageId)==7)
			{
				status = "searchFail";
			}
			else if(Integer.parseInt(pageId)==1)
			{
				status="failure";
			}
			else
			{
				status = "underFail";
			}
			
		}
		else
		{
			//System.out.println("manoj in finish else");
					
			session.removeAttribute("tempvec");
			session.removeAttribute("mailVec");
			session.removeAttribute("CopmId");
			session.removeAttribute("fCopmId");
			
			ActionErrors errors = new ActionErrors();
			errors.clear();
			errors.add("print", new ActionError("errors.print.finish"));
			
				saveErrors(request, errors);
			if(Integer.parseInt(pageId)==7)
			{
				status = "searchFinish";
			}
			else if(Integer.parseInt(pageId)==1)
			{
				status="finish";
			}
			else
			{
				status = "underFinish";
			}
			
		}
		// TODO Auto-generated method stub
		return mapping.findForward(status);
		
	}
}