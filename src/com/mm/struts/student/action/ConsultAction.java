



/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mm.struts.student.action;

import java.io.PrintWriter;
import java.util.Vector;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import com.mm.core.master.IndvMaster;


/** 
 * MyEclipse Struts
 * Creation date: 06-30-2007
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */
public class ConsultAction extends Action {
	
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) throws Exception {
		IndvMaster indvMaster= new IndvMaster();
		//int numCatId = (request.getParameter("category")!=null) ? Integer.parseInt(request.getParameter("category").trim()) : 0;
		int numconId = (request.getParameter("uid")!=null) ? Integer.parseInt(request.getParameter("uid").trim()) : 0;
		//////System.out.println("numCatId>>>>>>>>>>>"+numCatId);
		//////System.out.println("numbrandId>>>>>>>>>>>"+numbrandId);
		Vector conVec = indvMaster.getConsultants(getDataSource(request, "student"));
		//////System.out.println("brandVec>>>>>>>>>>>"+brandVec);
		String strBrand = getCollegeList(numconId, conVec);
		
	    response.setContentType("text/html;charset=ISO-8859-1");
	    PrintWriter out = response.getWriter();
	    out.println(strBrand);
	    out.flush();
		return null;
	}
	
	private String getCollegeList(int brandId, Vector conVec)
	{
		String strResult ="<select name=\"cons\"  tabindex=\"3\" >";
		for(int i=0; i<conVec.size(); i++)
		{
			Vector tempVec = (Vector)conVec.elementAt(i);
			int numBrandId = Integer.parseInt(tempVec.elementAt(0).toString().trim());
			String strfName = tempVec.elementAt(1).toString().trim();
			String strlName = tempVec.elementAt(2).toString().trim();
			//////System.out.println("numPlaceId "+numPlaceId);
			//////System.out.println("strPName "+strPName);
			if(brandId == numBrandId)
			{
				strResult += "<option value=\"" + numBrandId + "\" Selected>" + strfName + " " + strlName + "</option>";
			}
			else
			{
				strResult += "<option value=\"" + numBrandId + "\" Selected>" + strfName + " " + strlName + "</option>";
			}
		}
		strResult += "</select>";
		return strResult;
	}
}