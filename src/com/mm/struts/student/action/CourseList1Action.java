


/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */

package com.mm.struts.student.action;
import java.io.PrintWriter;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import java.util.Vector;
import com.mm.core.master.RootMaster;

/** 
 * MyEclipse Struts
 * Creation date: 06-30-2007
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */
public class CourseList1Action extends Action {
	RootMaster rootMaster= new RootMaster();
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) throws Exception {
		// TODO Auto-generated method stub
		String StrCourseId = (request.getParameter("courseId")!=null) ? (request.getParameter("courseId").trim()) : "0";
		////System.out.println("StrCourseId1............ "+StrCourseId);
		
		
		String numCount = (request.getParameter("numCount") != null) ? request.getParameter("numCount") : "2" ;	
		//System.out.println("numCount....CourseList1Action........ "+request.getParameter("numCount"));
		//System.out.println("numCount....CourseList1Action........ "+numCount);
		int count = Integer.parseInt(numCount);
		int remander = count%2;
		//System.out.println("numCount....CourseList1Action........ "+numCount);
		
		int tabindex = 0;
		
		int numCourseId=Integer.parseInt(StrCourseId);
		Vector CourseVec = rootMaster.getCourse(getDataSource(request,"student"));
		////System.out.println("CourseVec............ "+CourseVec);
	    String strCourse = getCourseList(numCourseId, CourseVec);
	   
	    
	    response.setContentType("text/html;charset=ISO-8859-1");
	    PrintWriter out = response.getWriter();
	    out.println(strCourse);
	    out.flush();
		return null;
	}
	
	private String getCourseList(int numCid, Vector CountryVec)
	{
		String strResult ="<select name=\"coures0\" tabindex=\"1\" onchange=\"retrieveStraem1URL('courseStreamList1.do?courseId='+this.value),doTask1(+this.value);\">";
		
		for(int i=0; i<CountryVec.size(); i++)
		{
			Vector tempVec = (Vector)CountryVec.elementAt(i);
			
			int numCIdTemp=Integer.parseInt(tempVec.elementAt(0).toString().trim());			
			String strCName = tempVec.elementAt(1).toString().trim();
			
			if(numCid == numCIdTemp)
			{
				strResult += "<option value=\"" + numCIdTemp + "\" Selected>" + strCName + "</option>";
			}
			else
			{
				strResult += "<option value=\"" + numCIdTemp + "\">" + strCName + "</option>";
			}
		}
		strResult += "</select>";
		////System.out.println("strResult............ "+strResult);
		
		return strResult;
	}
	
}