
/*

 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mm.struts.student.action;


import java.io.PrintWriter;
import java.util.Vector;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import com.mm.core.master.RootMaster;

/** 
 * MyEclipse Struts
 * Creation date: 06-30-2007
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */
public class CourseStreamList2Action extends Action {
	RootMaster rootMaster= new RootMaster();
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) throws Exception {
		
		int numSId = (request.getParameter("streamId")!=null) ? Integer.parseInt(request.getParameter("streamId").trim()) : 0;
		String courseStr = (request.getParameter("courseId")!=null) ? (request.getParameter("courseId").trim()) : "0";
		//System.out.println("courseStr2 ..CourseStreamAction..................."+courseStr);		
		//System.out.println("numSId ..CourseStreamAction..................."+numSId);	
		int numCId=Integer.parseInt(courseStr);
		
		int tabindex = 0;
		
		
		Vector streamVec = rootMaster.getStream(numCId, getDataSource(request, "student"));
	    String strStream = getCourseStreamList(numSId, numCId, streamVec);
	    
	   
	    
	    ////////System.out.println("States "+strState);
	    response.setContentType("text/html;charset=ISO-8859-1");
	    PrintWriter out = response.getWriter();
	    out.println(strStream);
	    out.flush();
		return null;
	}
	private String getCourseStreamList(int numSid, int numCId, Vector streamVec)
	{
		String strResult ="<select name=\"msub1\" tabindex=\"9\" onchange=\"showOth2(+this.value);\">";
		
		for(int i=0; i<streamVec.size(); i++)
		{
			Vector tempVec = (Vector)streamVec.elementAt(i);
			int numStateId = Integer.parseInt(tempVec.elementAt(0).toString().trim());
			String strSName = tempVec.elementAt(1).toString().trim();
			////////System.out.println("numStateId "+numStateId);
			////////System.out.println("strSName "+strSName);
			if(numSid == numStateId)
			{
				strResult += "<option value=\"" + numStateId + "\" Selected>" + strSName + "</option>";
			}
			else
			{
				strResult += "<option value=\"" + numStateId + "\">" + strSName + "</option>";
			}
		}
		strResult += "</select>";
		return strResult;
	}
	
	
}