/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.mm.struts.corpo.action;

import java.io.PrintWriter;
import java.util.ArrayList;
import java.util.HashMap;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
//import javax.sql.DataSource;
import com.mm.core.master.RootMaster;

/** 
 * MyEclipse Struts
 * Creation date: 06-30-2007
 * 
 * XDoclet definition:
 * @struts.action validate="true"
 */
public class SearchCityAction extends Action {
	RootMaster rootMaster= new RootMaster();
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 * onchange=\"retrieveCollListURL('collegeListForSearch.do?collcountry="+numCId+"&collstate="+numSId+"&collcity='+this.value), doTaskReg1(+this.value)\">";
	 */
	
	
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) throws Exception {
		//String codeStr = (request.getParameter("country")!=null) ? (request.getParameter("country").trim()) : "0~0";
		//String codeStrArr[]= codeStr.split("~");
		//int numCId=Integer.parseInt(codeStrArr[0]);
		String codeStr = (request.getParameter("country")!=null) ? (request.getParameter("country").trim()) : "0~0";
		String codeStrArr[]= codeStr.split("~");
		int numCId=Integer.parseInt(codeStrArr[0]);
		
		int numSId = (request.getParameter("state")!=null) ? Integer.parseInt(request.getParameter("state").trim()) : 0;
		int numPId = (request.getParameter("place")!=null) ? Integer.parseInt(request.getParameter("place").trim()) : 0;
		
		//System.out.println("numSId...@@@@@@@@@@@@@@..........."+numSId);
		String strPlace = "";
		if((request.getParameter("state")!=null)&& (numSId!=0))
		{	
			//System.out.println("if...@@@@@@@@@@@@@@...........");
			
			HttpSession session = request.getSession();
			HashMap citiesList = (HashMap)session.getAttribute("AllCities");
			strPlace = getPlaceList(numPId, numSId, citiesList,numCId);
		}
		else
		{	
			//System.out.println("else...@@@@@@@@@@@@@@...........");
			
			strPlace ="<select name=\"city\"  tabindex=\"11\" onchange=\"retrieveCollListURL('collegeListForSearch.do?collcountry="+numCId+"&collstate="+numSId+"&collcity='+this.value), doTaskReg1(+this.value)\">";
			strPlace += "<option value=\"0\" Selected>Select</option>";
			//strPlace += "<option value=\"-1\">Others</option>";
			strPlace += "</select>";
			
		}
	    response.setContentType("text/html;charset=ISO-8859-1");
	    PrintWriter out = response.getWriter();
	    out.println(strPlace);
	    out.flush();
		return null;
	}
	
	private String getPlaceList(int numPId, int numSId, HashMap citiesList,int numCId)
	{
		ArrayList AllCities = (ArrayList)citiesList.get(numSId);
		String strResult ="<select name=\"city\"  tabindex=\"11\" onchange=\"retrieveCollListURL('collegeListForSearch.do?collcountry="+numCId+"&collstate="+numSId+"&collcity='+this.value), doTaskReg1(+this.value)\">";
				
		for(int i=0; i<AllCities.size()-1; i++)
		{
			ArrayList CitiesArr = (ArrayList)AllCities.get(i);
			int numPlaceId = Integer.parseInt(CitiesArr.get(0).toString().trim());
			String strPName = CitiesArr.get(1).toString().trim();
			if(numPId == numPlaceId)
			{
				strResult += "<option value=\"" + numPlaceId + "\" Selected>" + strPName + "</option>";
			}
			else
			{
				strResult += "<option value=\"" + numPlaceId + "\">" + strPName + "</option>";
			}
		}
		strResult += "</select>";
		return strResult;
	}
}